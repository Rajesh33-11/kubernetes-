## Verfiy List of Clusters
```
kops get cluster
```
<img width="783" height="185" alt="image" src="https://github.com/user-attachments/assets/953474d8-9142-4184-93d5-a7b5db2b7879" 

===================================================================================
## Verfiy List of Pods
```
kubectl get Pods
```
```
kubectl get Po
```
```
kubectl get Pod
```
<img width="881" height="216" alt="image" src="https://github.com/user-attachments/assets/3b91eb4e-4c5c-456e-ba0d-aa34c49e7800" />

===================================================================================
How many ways can we create a Pod in Kubernetes?

👉 There are mainly 3 ways to create a Pod.

I’ll explain each one clearly, with examples.

1️⃣ Imperative way (Direct CLI command)

This is the fastest and most commonly used for testing and learning.

Example:
kubectl run nginx-pod --image=nginx


✔ Creates a pod directly
❌ Not recommended for production
❌ No YAML, so not reusable

👉 Mostly used for:

Quick testing

Troubleshooting

Demos

2️⃣ Declarative way (Using YAML file) ⭐ BEST PRACTICE

This is the recommended and production way.

Step 1: Create a YAML file
apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
spec:
  containers:
  - name: nginx
    image: nginx

Step 2: Apply it
kubectl apply -f pod.yaml


✔ Reusable
✔ Version controlled
✔ Production-ready
✔ Used in real projects

👉 Always use this method in real environments

3️⃣ Via Controllers (Indirect way)

Pods are rarely created directly in real projects.

Instead, they are created using:

Deployment

ReplicaSet

DaemonSet

Job / CronJob

Example (Deployment):

kubectl create deployment nginx-deploy --image=nginx


➡ Kubernetes internally creates:

Deployment → ReplicaSet → Pod


✔ Auto-healing
✔ Scaling supported
✔ Rolling updates
✔ Industry standard

📌 Interview-Friendly Answer (IMPORTANT)

Q: How many ways can you create a Pod?

✅ Answer:

We can create a Pod in three ways:

Imperative command using kubectl

Declarative way using YAML manifest

Indirectly via controllers like Deployment or ReplicaSet
